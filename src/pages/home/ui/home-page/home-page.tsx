import { FC } from 'react';

import { PageWrapper } from '@/shared/components/page-wrapper';

const HomePage: FC = () => {
  return (
    <PageWrapper>
      HomePage
    </PageWrapper>
  );
};

export default HomePage;
